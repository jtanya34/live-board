import { createSelector } from 'reselect';

export const set_session_variable = state => state.sessions.set_session_variable;

export const sessions_list_status = state => state.sessions.sessions_list_status;